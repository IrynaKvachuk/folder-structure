import Container from '../Container/Container';

const Header: React.FC = () => {
  return (
    <Container>
      <header className="app_header">
        <div className="app_header-logo">Folder Structure</div>
      </header>
    </Container>
  );
};

export default Header;
